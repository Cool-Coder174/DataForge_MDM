# DataForge MDM - core Python dependencies for local development and CI.
#
# NOTE: AWS Glue jobs depend on `awsglue` and `pyspark`, which are provided by
# the managed Glue runtime and are NOT installed here. Lambda functions pin
# their own runtime deps under lambda/*/requirements.txt.

# AWS SDK
boto3==1.34.103
botocore==1.34.103

# Data processing
pandas==2.2.2
pyarrow==16.1.0

# RDS / PostgreSQL access (binary wheel bundles libpq)
psycopg2-binary==2.9.9

# HTTP client
requests==2.32.2

# Testing
pytest==8.2.0

# Linting / validation (used by CI)
pyflakes==3.2.0
sqlfluff==3.0.7
cfn-lint==1.3.0
