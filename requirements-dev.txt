# Local dev + CI dependencies (NOT the Lambda runtime deps; those live per-function)
pytest==8.2.0
pyflakes==3.2.0
boto3==1.34.103
botocore==1.34.103
pandas==2.2.2
pyarrow==16.1.0
psycopg2-binary==2.9.9
requests==2.32.2
# SQL linting (best-effort, used by CI)
sqlfluff==3.0.7
# cfn-lint for template validation in CI without AWS creds
cfn-lint==1.3.0
